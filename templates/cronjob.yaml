---
{{- if .Values.plone.zodbpack.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "plone.fullname" . }}-zodbpack
  labels:
    {{- include "plone.labels" . | nindent 4 }}
    app.kubernetes.io/component: zodbpack
spec:
  schedule: {{ .Values.plone.zodbpack.schedule | quote }}
  successfulJobsHistoryLimit: {{ .Values.plone.zodbpack.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.plone.zodbpack.failedJobsHistoryLimit }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "plone.labels" . | nindent 12 }}
            app.kubernetes.io/component: zodbpack
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: OnFailure
          {{- with .Values.podSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: zodbpack
              {{- with .Values.securityContext }}
              securityContext:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- with .Values.plone.workingDir }}
              workingDir: {{ . }}
              {{- end }}
              command:
                - /bin/sh
                - -c
                - {{ .Values.plone.zodbpack.command | quote }}
              {{- with .Values.plone.env }}
              env:
                {{- range $key, $value := . }}
                - name: {{ $key }}
                  value: {{ tpl ($value | toString) $ | quote }}
                {{- end }}
              {{- end }}
              {{- if .Values.plone.envFrom }}
              {{- with .Values.plone.envFrom }}
              envFrom:
                {{- tpl (toYaml .) $ | nindent 16 }}
              {{- end }}
              {{- end }}
              {{- with .Values.plone.zodbpack.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- if or .Values.volumeMounts .Values.blobstorage.enabled }}
              volumeMounts:
                {{- with .Values.volumeMounts }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
                {{- if .Values.blobstorage.enabled }}
                - name: blobstorage
                  mountPath: {{ .Values.blobstorage.mountPath }}
                {{- end }}
              {{- end }}
          {{- if or .Values.volumes .Values.blobstorage.enabled }}
          volumes:
            {{- with .Values.volumes }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.blobstorage.enabled }}
            - name: blobstorage
              persistentVolumeClaim:
                claimName: {{ if .Values.blobstorage.persistence.existingClaim }}{{ .Values.blobstorage.persistence.existingClaim }}{{ else }}{{ include "plone.fullname" . }}-blobstorage{{ end }}
            {{- end }}
          {{- end }}
{{- end }}
